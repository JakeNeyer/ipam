# Copy to .env and fill in values, then run:
#   set -a && source .env && set +a
#   ./hack/terraform-example.sh plan
# Or export manually:
#   export IPAM_ENDPOINT=http://localhost:8011
#   export IPAM_TOKEN=your-api-token

# Base URL of the IPAM API (no trailing slash).
IPAM_ENDPOINT=http://localhost:8011

# API token from IPAM (Admin > API tokens). Must not contain double quotes (").
IPAM_TOKEN=
