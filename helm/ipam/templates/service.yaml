apiVersion: v1
kind: Service
metadata:
  name: {{ include "ipam.fullname" . }}
  labels:
    {{- include "ipam.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ipam.selectorLabels" . | nindent 4 }}
